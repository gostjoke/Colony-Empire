# Colony Empire 教學 — 第 10b 課
## 單位美術：殖民者、斥候、衛兵（自動套國家顏色）

這一課把單位從「圓形代幣＋字母」換成真正的角色圖：

| 單位 | 造型 | 國家色的位置 | 遊戲數值 |
|---|---|---|---|
| **Colonist 殖民者**（原 Settler） | 寬邊氈帽、背鋪蓋捲、手拿鏟子 | 外套 | 移動 2，按 B 建城 |
| **Scout 斥候**（新單位） | 羽毛軟帽、皮大衣、舉望遠鏡 | 披風、羽毛、領巾 | 移動 3、視野 3；拜訪村莊時禮物 ×1.5、好感 +15 |
| **Guard 衛兵**（原 Soldier） | 鋼盔插羽、胸甲、長戟、圓盾 | 上衣、羽飾、盾面 | 移動 2，駐守城市；西班牙成本 -40% |

開局單位改成：殖民者、衛兵、斥候、工人。工人還沒有圖，暫時沿用圓形代幣。

---

## Step 1：更新

1. 把新的 `godot_starter/main.gd` 貼上。
2. 圖片已經放在 `godot_starter/assets/units/`。打開 Godot 編輯器時會自動匯入，然後按 **F5**。
3. 第 10 課的存檔可以直接讀：舊的 `soldier` 會自動轉成 `guard`。
4. 城市生產鍵變成 **1–8**：1 殖民者、2 工人、3 斥候、4 衛兵、5–8 建築。

---

## Step 2：一張圖怎麼變成四個國家的顏色

每個單位有兩張 PNG：

| 檔案 | 內容 |
|---|---|
| `unit_scout.png` | 完整的圖，但國家色的部分畫成**灰色**（亮灰＋陰影灰） |
| `unit_scout_team.png` | 遮罩：白色 = 要上國家色的地方 |

遊戲第一次畫某國的某種單位時，`_unit_texture()` 會逐一處理每個像素：

```
新顏色 = lerp(原色, 原色 × 國家色, 遮罩)
```

灰色乘上國家色以後，亮灰變成國家色，陰影灰變成較深的國家色，**陰影會保留下來**。算好的圖存在 `unit_tex` 裡，之後直接重用。三種單位 × 三個國家全部算完只要大約 10 毫秒。

所以新增國家或改國家顏色時，**不需要重畫任何圖**。

---

## Step 3：畫在地圖上

- 腳底畫一個國家色的橢圓底座（Civ 風格），選中的單位外圈會有黃色脈動。
- 同一格有好幾個單位時，只畫一個（優先畫選中的，其次是你自己的），右下角顯示數量徽章。再點一次這一格可以輪流切換。
- 已經沒有移動力、睡眠中或工作中的單位會畫暗一點。
- `texture_filter = LINEAR_WITH_MIPMAPS` 搭配 `generate_mipmaps()`，縮小地圖時圖片不會出現鋸齒或閃爍。

---

## Step 4：自己修改或新增角色

原始檔在 `art_src/`：

- `unit_*.svg`：向量原稿，可以用 Inkscape 等軟體打開修改。
- `make_units.py`：產生器，所有角色都用 Python 寫成 SVG。

```
pip install cairosvg pillow numpy
python art_src/make_units.py
```

執行後會重新產生 `godot_starter/assets/units/` 裡的 PNG 和遮罩。規則只有一條：**要上國家色的地方，填色用 `T1`（亮）或 `T2`（陰影）**，遮罩會自動產生。

新增角色的步驟（例如工人）：

1. 在 `make_units.py` 寫一個 `worker()` 函式（可以參考 `colonist()`，共用 `legs()`、`torso()`、`head()`、`limb()`）。
2. 把它加進 `UNITS = {...}`。
3. 執行腳本。遊戲會自動找到 `unit_worker.png`，不用改任何 GDScript。

圖的規格：128×128，腳底大約在 y=117（圖高的 91%），頭頂不要超出 y=0。
