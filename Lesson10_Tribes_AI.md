# Colony Empire 教學 — 第 10 課
## 美洲部落村莊 × AI 殖民國家

新大陸上現在不只有你一個國家：

| 新內容 | 說明 |
|---|---|
| **美洲部落** | 4 個部落，每個 5 座村莊，擁有周圍的土地，對每個殖民國家各有一個好感度（-100 ~ 100） |
| **AI 殖民國家** | 預設 2 個對手（西班牙、法國），會擴張建城、蓋農場礦場、駐軍、研究科技、送禮給部落、用金幣買生產 |
| **國家特性** | 依 GDD 6.1：英=人口成長、西=軍事、法=部落外交、荷=貿易 |
| **視野** | 只有在你的單位或城市附近，才看得到外國單位；第一次看到時會觸發「相遇」 |
| **外交面板** | 按 **D**：各國的城市、人口、分數，以及各部落的好感和貿易收入 |
| **結算排名** | 1811 年依分數排出所有國家的名次 |

---

## Step 1：更新程式

1. `main.gd` → 全選 → 刪除 → 貼上新的 `godot_starter/main.gd` → 存檔 → **F5**。
2. 地圖放大到 70×44，讓三個國家加上部落都有空間。
3. 存檔檔名改成 `user://save_v10.json`，第 9 課的存檔不相容。
4. 想玩別的國家：把 `PLAYER_NATION` 改成 1（西班牙）、2（法國）或 3（荷蘭）；對手數量用 `AI_COUNT` 調整（最多 3）。

### 新增操作

| 按鍵 | 功能 |
|---|---|
| 右鍵點村莊 | 單位自動走到村莊旁邊（村莊本身不能進入） |
| 走到村莊旁 | 第一次拜訪時酋長會送禮（金幣、地圖、研究點數，或給城市糧食） |
| T | 單位在村莊旁邊時，送 20 金幣給部落（好感 +15） |
| D | 開啟或關閉外交面板 |

地圖上的標示：村莊是三頂帳篷，**?** 代表還沒拜訪過；細框是部落土地，粗框是殖民地國界，顏色代表所屬國家或部落。

---

## Step 2：從「單一玩家」改成「多國」

這一課最大的改動是資料結構。第 9 課的 `gold`、`research`、`tech` 都是全域變數，現在全部改放在 `nations` 陣列裡：

```gdscript
var nations := []   # [0] = 玩家；每個：{ def, gold, research, tech, met, founded }
```

每個城市和單位都多了 `owner` 欄位（0 = 玩家）。所有會用到科技的計算都要傳入國家編號，例如：

```gdscript
func tile_yield(c: Vector2i, n: int = 0) -> Vector3i
```

原則是：**遊戲規則只寫一份，玩家和 AI 共用**。城市結算 `_process_city()`、建城 `_found_city()`、工人 `_start_job()` 都不分玩家或 AI，只有「顯示訊息」和「自動選下一個單位」這類操作會先檢查 `owner == 0`。

---

## Step 3：美洲部落

### 生成

`_place_tribes()` 先為每個部落找一個**家園**（距離殖民者起點至少 7 格、與其他部落距離至少 9 格），再在家園周圍 5 格內放 5 座村莊，村莊之間至少相隔 3 格。每座村莊佔有旁邊一圈土地，存在 `tribe_land`。

- 部落土地**不能建城**，你的國界也不會擴張到上面。
- 村莊格在尋路裡是不可通行的（`_passable()` 會排除）。

### 好感度會因為什麼改變

| 事件 | 好感變化 |
|---|---|
| 初始值 | +10（法國 +30） |
| 第一次拜訪村莊 | +10，並獲得一份禮物 |
| 送禮（T，20 金幣） | +15（法國 +20） |
| 在村莊 4 格內建城 | 每個受影響的部落 -15（法國 -7） |
| 負值每回合 | 有 25% 機率 +1（怨恨會慢慢消退） |

### 效果

| 好感 | 狀態 | 效果 |
|---|---|---|
| ≥ 50 | Allied | — |
| ≥ 20 | Friendly | 每座村莊若在你某城市 6 格內，每回合 +1 金（荷蘭 +2） |
| -19 ~ 19 | Wary | — |
| -39 ~ -20 | Angry | — |
| ≤ -40 | Hostile | 每回合 20% 機率**突襲**最近的城市：搶走金幣、燒掉一個農場或礦場 |

---

## Step 4：AI 殖民國家

每回合順序是：玩家結束回合 → 所有城市結算 → 部落 → 所有單位繼續移動 → **每個 AI 執行 `_ai_turn(n)`**。

### 城市要蓋什麼（`_ai_pick_build`）

依序檢查，符合就選：

1. 城市數還沒到上限，且目前沒有開拓者 → **開拓者**
2. 工人比城市少 → **工人**
3. 士兵比城市少 → **士兵**
4. 缺哪棟建築就蓋：糧倉 → 市場 → 工坊 → 學校
5. 人口 ≥ 4 且還能擴張 → 再出一個開拓者

金幣比「購買價格 + 80」還多時，AI 會直接花錢買下目前的生產。

### 開拓者選址（`_ai_find_site`）

在 9 格內（找不到就擴大到 16 格）評分每一個能建城的格子：

```
分數 = 周圍 2 格的地塊產出（糧×3 + 產×2 + 金）
     + 靠海 +4
     - 靠近討厭自己的部落村莊 -8（沒有交惡的村莊 -2）
     - 距離 × 2
```

取分數最高、而且走得到的格子，走過去建城。第一座城會直接建在登陸點。

### 工人與士兵

- **工人**：在自己國界內找最近、還沒改良的格子，草原或平原蓋農場，丘陵蓋礦場。
- **士兵**：找一座沒有守軍的城市進駐並睡眠；每座城市都有守軍時，就在附近巡邏。
- **研究**：點數一夠就依序買科技。
- **外交**：金幣 ≥ 60 時，挑好感最低的部落送禮。

> 用 `goal` 欄位記錄單位的目的地，`_goal_taken()` 可以避免兩個單位搶同一個目標。

---

## Step 5：視野與相遇

- `seen`：探索過的地形（永久）。
- `in_sight`：此刻看得到的格子（單位 2 格、城市 3 格），每次移動和每回合結束時用 `_recompute_vision()` 重新計算。

外國單位只有在 `in_sight` 裡才會畫出來。外國城市和村莊只要探索過就會一直顯示。第一次看到某國的單位或城市時，會觸發 `_meet()`。

---

## Step 6：國家特性

| 國家 | 特性 | 實作位置 |
|---|---|---|
| England | 成長需求 -20% | `growth_need()` |
| Spain | 士兵成本 -40% | `build_cost()` |
| France | 部落初始好感 +20、送禮多 +5、建城懲罰減半 | `_place_tribes()`、`_gift()`、`_found_city()` |
| Netherlands | 部落貿易 ×2、每座城市 +1 金 | `_trade_income()`、`city_yield()` |

---

## 可調整的常數

| 常數 | 預設 | 作用 |
|---|---|---|
| `AI_COUNT` | 2 | 對手數量 |
| `VILLAGES_PER_TRIBE` | 5 | 每個部落的村莊數 |
| `TRADE_ATT` / `TRADE_RANGE` | 20 / 6 | 貿易門檻 |
| `RAID_ATT` | -40 | 開始突襲的好感值 |
| `LAND_GRAB` | 15 | 建城太近的懲罰 |
| `NATIONS[i].max_cities` | 6–8 | AI 擴張上限（也可以看成 AI 的「擴張型 / 守成型」性格） |

---

## 小練習

1. 把 `AI_COUNT` 改成 3，讓荷蘭也加入。
2. 新增村莊互動「學習技能」：好感 ≥ 50 時按 L，讓工人永久加快 1 回合。
3. 讓 AI 在好感 ≤ -20 的部落附近完全不建城（修改 `_site_score`）。
4. 外交面板加一欄「各部落對 AI 國家的好感」，觀察 AI 怎麼經營部落關係。

## 下一課預告

**第 11 課：戰鬥**：攻擊與防禦（戰力 + 地形加成 + 隨機）、城市守軍、占領城市、對 AI 宣戰與議和、敵對部落派出戰士。
