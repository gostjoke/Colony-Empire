# Colony Empire 教學 — 第 9 課
## 六角格地圖 × 回合制城市建造大戰略

這一課把遊戲從「即時的等距小鎮經營」改成**文明帝國式的六角格大戰略**：

| 項目 | 舊版（第 1–8 課） | 新版（第 9 課） |
|---|---|---|
| 地圖 | 56×56 等距菱形格 | 60×40 **尖頂六角格**，四周是海的「新大陸」 |
| 時間 | 即時，每 1.5 秒結算 | **回合制**，按 Enter 結束回合，1 回合 = 2 年（1500 → 1811） |
| 核心單位 | 房子 / 農場 / 貿易站蓋在格子上 | **城市**：人口、領土、耕作格、生產列 |
| 移動者 | 自動亂走的工人 | 可操控的**單位**：開拓者、工人、士兵 |
| 農場 / 道路 | 直接點格子蓋 | 由**工人**花數回合建造（農場、礦場、道路、伐林） |
| 視野 | 全圖可見 | **戰爭迷霧**，要派兵探索 |
| 勝利 | 人口 25 | 暫定：1811 年結算分數（獨立戰爭之後的課加） |

---

## Step 1：更新程式

1. 打開 `main.gd` → 全選 → 刪除 → 貼上新的 `godot_starter/main.gd` → 存檔 → **F5**。
2. `main.tscn` 不用改。
3. 地形改成**用程式直接畫六角形**，`assets/` 裡的等距 PNG 暫時不用（先留著，之後畫六角格美術時再替換）。
4. 存檔檔名改成 `user://save_hex.json`，不會讀到舊版不相容的存檔。

### 操作方式

| 按鍵 | 功能 |
|---|---|
| 左鍵 | 選取單位 / 城市（同一格再點一次會輪流切換） |
| 右鍵 | 移動選中的單位（滑鼠停著會先顯示路線與幾回合到） |
| Enter | 結束回合 |
| Tab / N | 下一個待命單位 |
| B | 開拓者建立城市 |
| F / M / R / C | 工人：農場 / 礦場 / 道路 / 砍伐森林 |
| Space / H | 單位本回合跳過 / 睡眠 |
| 1–7 | （選城市時）選擇生產項目 |
| G | （選城市時）用金幣直接買下目前生產 |
| Q / W / E | 用研究點數解鎖科技 |
| 方向鍵、中鍵拖曳、滾輪 | 平移、縮放 |
| F5 / F9 / F2 | 存檔 / 讀檔 / 新地圖 |

---

## Step 2：六角格座標（本課最重要的觀念）

### 軸座標 axial (q, r)

六角格有 6 個鄰居。用「軸座標」時，6 個方向永遠是固定的加減量，不用管奇偶列：

```gdscript
const DIRS := [Vector2i(1, 0), Vector2i(0, 1), Vector2i(-1, 1),
		Vector2i(-1, 0), Vector2i(0, -1), Vector2i(1, -1)]
```

地圖本身是長方形，所以產生地圖時用「行列（offset）」跑迴圈，再轉成 axial 存進字典：

```gdscript
func offset_to_axial(col: int, row: int) -> Vector2i:
	return Vector2i(col - (row >> 1), row)     # 奇數列往右錯開半格
```

### 六角格 → 螢幕座標

```gdscript
func hex_to_world(c: Vector2i) -> Vector2:
	return Vector2(HEX * SQ3 * (c.x + c.y * 0.5), HEX * 1.5 * c.y)
```

`HEX` 是中心到角的距離。左右鄰居相距 `√3 × HEX`，上下列相距 `1.5 × HEX`。

### 螢幕 → 六角格（滑鼠點哪一格）

先算出**小數**的 q、r，再做「立方座標四捨五入」：把 q、r、s（s = -q-r）各自四捨五入，誤差最大的那個用另外兩個重算，保證 q+r+s = 0。

```gdscript
if dq > dr and dq > ds: q = -r - s
elif dr > ds:           r = -q - s
```

### 距離與範圍

```gdscript
func hex_dist(a, b):
	var dq := a.x - b.x
	var dr := a.y - b.y
	return (absi(dq) + absi(dr) + absi(dq + dr)) / 2
```

`hexes_in_range(c, n)` 回傳 n 格內所有六角格，城市領土、視野、城市間距都用它。

---

## Step 3：回合制迴圈

即時的 `_economy_tick()` 被 `_end_turn()` 取代，順序很重要：

1. 每座城市結算：糧食 → 成長/飢荒、文化 → 擴張邊界、生產 → 完成單位或建築
2. 工人工作倒數，完成就放上農場/礦場/道路
3. 隨機事件（豐收、歐洲商船、移民、疾病、火災）
4. `turn += 1`，所有單位補滿移動力，**多回合移動命令自動繼續走**
5. 自動選取下一個要下命令的單位；沒有的話就跳到「還沒選生產」的城市

---

## Step 4：城市

- **領土**：建城時取得半徑 1；文化累積後擴到半徑 2、3（`territory` 字典記每格屬於哪座城）。
- **耕作格**：人口 N 就自動耕作領土中最好的 N 格（偏好糧食，所以城市會長大）。城市中心至少 2 糧 1 產 1 金。選中城市時，被耕作的格子會畫白圈。
- **成長**：每人口吃 2 糧，剩餘存起來，達到 `8 + 5 × 人口` 就 +1 人口。
- **生產**：沒選生產時產能會浪費，所以一定要選。開拓者需要城市人口 ≥ 2，完成時人口 −1。
- 城市之間至少距離 4 格（跟文明帝國一樣，防止擠在一起）。

---

## Step 5：單位與尋路

第 8 課的 `AStarGrid2D` 只能用方格，所以改用通用的 `AStar2D`，自己把六角格連起來：

```gdscript
for i in 3:   # 只連 E、SE、SW，另外三個方向由鄰居那邊連
	var n: Vector2i = c + DIRS[i]
	if _passable(n):
		astar.connect_points(_cid(c), _cid(n))
```

**小技巧**：把點的位置縮放成「鄰居相距 0.5」，權重設成「移動成本 × 2」，這樣 A* 算出的路徑成本剛好等於消耗的移動力，而且啟發函式不會高估（道路成本 0.5 也正確）。

移動成本：平原/草原 1、森林/丘陵 2、道路和城市 0.5、海/山不可通行。跟文明帝國一樣，只要還有移動力就一定能再走一步。

---

## Step 6：戰爭迷霧

`seen` 字典記錄探索過的格子。單位看得到 2 格、城市 3 格。沒探索的格子畫成深色，你的殖民者從東岸登陸（歐洲在東邊），要派士兵往內陸探索。

---

## 舊系統怎麼對應到新系統

| 舊 | 新 |
|---|---|
| House（人口容量） | 城市人口，靠糧食成長 |
| Farm 建築 | 工人蓋的「農場」地形改良 |
| TradePost | 城市建築 Market（+3 金） |
| 點森林清除得木頭 | 工人「砍伐森林」→ 變平原，所在城市 +12 生產 |
| 道路 | 工人蓋，移動成本變 0.5 |
| 季節 | 移除（1 回合 = 2 年，季節沒意義了） |
| 科技 Q/W/E | 保留，改成 Irrigation / Iron Tools / Vaccines |

---

## 小練習

1. 把 `MAP_W`、`MAP_H` 改成 80×50，看看大地圖的感覺。
2. 新增一種地形「沼澤 swamp」（2 糧 0 產、移動成本 3）。
3. 新增建築「Dock 碼頭」：城市鄰海時才能蓋，海洋格 +1 糧。
4. 讓 `_tile_score()` 在糧食已經夠多時改成偏好生產。

## 下一課預告

依照 GDD 的路線圖：
- **第 10 課**：美洲部落村莊 + 第一個 AI 殖民國家（簡單擴張 AI）
- **第 11 課**：戰鬥（戰力對比 + 地形加成）與城市防禦
- **第 12 課**：與歐洲貿易、獨立支持度、宣布獨立 → 獨立戰爭勝利
