# Colony Empire 教學 — 第 11b 課
## 介面改版：經典 4X 策略遊戲風格（參考文明 4 的版面與質感）

**方向**：捨棄原本的「深木＋黃銅」，改成經典 4X 的**深藍灰玻璃面板＋斜角金框**。按鈕和進度條有光澤，文字是白色並帶陰影。

所有圖都是程式畫的**原創素材**，沒有使用任何文明 4 的圖檔。參考的只有版面配置和質感方向。

| 區塊 | 改了什麼 |
|---|---|
| 所有面板 | 9-slice 圖片：藍灰漸層、上半部玻璃反光、金色斜角框、內側暗溝 |
| 按鈕 | 鋼藍色光澤按鈕＋金邊；滑鼠移上去變亮；目前選取的項目有金色光暈；**End Turn / Next Unit / Set Sail** 是金色大按鈕 |
| 進度條 | 暗色凹槽＋光澤填色（同一張白色圖，依用途染成綠、橘等顏色） |
| 頂部資源列 | 深藍漸層＋底部金線 |
| 事件通知 | 小卡片；壞消息用暗紅卡片 |
| 地圖上的城市名牌 | 仿 4X 城市橫幅：左邊是**人口圓章**（850 / 4.5k / 12k；外圈綠色＝上回合成長、紅色＝衰退），中間是城名，右邊是**生產剩幾回合**（還沒選要蓋什麼就顯示紅色「!」），下方是生產進度條 |
| 城市畫面 | 改成 4X 城市畫面的配置（見下） |

### 城市畫面新配置

```
┌───────────────────────────────────────────────────────────┐
│ 人口 · 疆界 · 建築數      [<]  NEW HAVEN  [>]     [Close] │
│ [糧食條：Fed 100% · 糧價 · 糧倉]  [生產條：項目 · 進度 · 回合] │
├──────────────┬────────────────────────┬───────────────────┤
│ People       │                        │ Buildings         │
│ Yields       │     2.5D 城市模型       │ （已擁有的建築）   │
│ Work         │                        │                   │
├──────────────┴────────────────────────┴───────────────────┤
│ [目前生產＋購買]     [建造格：方形圖示按鈕，按 1–8 也可以]   │
└───────────────────────────────────────────────────────────┘
```

---

## Step 1：更新檔案

1. 更新 `hud.gd`、`main.gd`。
2. 新增 `tools/make_ui_skin.py` 和它產生的 20 張 `assets/ui/skin_*.png`。
   - 想改顏色或邊框，就改這支 Python 再重跑：`python tools/make_ui_skin.py assets/ui`
   - 需要 Pillow：`pip install pillow`
3. 新增 `shot_test.gd`：自動截圖標題、地圖、單位、科技、城市畫面，方便比對 UI 改動（需要有顯示器）。

## Step 2：9-slice 是什麼

每張面板圖只有 32–64 px。Godot 的 `StyleBoxTexture` 會把四個角（`texture_margin`）保持原樣，只拉伸中間。所以同一張圖可以撐成任何大小的面板，金框也不會變形。

`hud.gd` 裡只有一個函式負責這件事：

```gdscript
func _skin(name: String, margin := -1.0, tint := Color(1, 1, 1)) -> StyleBoxTexture
```

- `SKIN_MARGIN` 表記錄每張圖的角有多大。
- `tint` 讓同一張白色光澤圖變成不同顏色的進度條。
- 主題（Theme）裡的 PanelContainer、Button、ProgressBar、Tooltip 全部改用 `_skin(...)`。
- 所有 Label 預設都有 1–2 px 的陰影，在地圖上也看得清楚。

## Step 3：城市名牌（`main.gd` → `_draw_city_label`）

名牌直接畫在地圖上，由以下幾部分組成：

- 圓角 `StyleBoxFlat`：國家色加深、細金邊、陰影、上半部反光。
- 人口圓章：`draw_circle` 疊出黑邊、彩色外圈、國家色、反光。
- 右邊的鎚子圖示和剩餘回合數。
- 橘色生產進度條。
