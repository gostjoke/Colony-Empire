# Colony Empire 教學 — 第 10c 課
## 2.5D：傾斜的立體六角地圖 × 預先渲染的 3D 單位

地圖和單位都改成 2.5D，做法跟經典 2.5D 策略遊戲一樣：**遊戲本身仍是 2D**，但畫面看起來是立體的。

| | 之前（10b） | 現在（10c） |
|---|---|---|
| 地圖 | 正上方俯視的平面六角格 | 斜 37° 俯視：地面壓扁、每格是有高度的六角柱，前方有土牆 |
| 地形 | 平面上畫三角形樹、山 | 3D 松樹和闊葉樹、雪山、丘陵、有城牆和教堂的城鎮、帳篷村落 |
| 單位 | 正面平面插圖 | 3D 小模型從同一個角度拍下來，有光影；加上工人，四種單位都有圖 |
| 前後遮擋 | 無 | 由後往前畫，前面的山、樹、單位會擋住後面的 |

---

## Step 1：更新

1. 貼上新的 `godot_starter/main.gd`。
2. 新的圖片在 `godot_starter/assets/sprites/`（16 張）。打開 Godot 編輯器時會自動匯入，然後按 **F5**。
3. 第 10 / 10b 課的 `assets/units/` 和 `art_src/`（SVG 平面版）已經用不到，可以刪掉，也可以留作紀念。
4. 遊戲規則沒有變，第 10 課的存檔可以直接讀取。

---

## Step 2：2.5D 投影，只要兩個數字

假設相機從 37° 往下看（正交投影）：

- 地面的深度只剩 **sin 37° = 0.6** → `TILT`
- 往上的高度只剩 **cos 37° = 0.8** → `RISE`

```gdscript
func _flat(w: Vector2) -> Vector2:              # 地面上的點
	return origin + Vector2(w.x, w.y * TILT) * zoom

func hex_to_screen(c: Vector2i) -> Vector2:     # 六角柱頂面的中心（單位站的位置）
	return _flat(hex_to_world(c)) - Vector2(0, _lift(c))   # _lift = 高度 × RISE
```

每種地形有自己的高度（`ELEV`）：海 0，平地 0.16，丘陵 0.32，山 0.4。

---

## Step 3：畫一個六角柱

1. **前牆**：頂面的東南邊（角 1→2）和西南邊（角 2→3）往下延伸到海平面，一面深、一面淺。
2. **頂面**：`draw_polygon(點, 顏色陣列)`，六個角各給一個亮度（`LIGHT`），模擬光從左上方照過來。算好的顏色會快取在 `tile_cols`。
3. **頂面上的東西**：農田條紋、道路、國界、耕作圈、滑鼠停留的高亮，都畫在頂面上。

## Step 4：前後遮擋（畫家演算法）

```
for 每一列 row（從畫面上方＝後面，往下＝前面）:
    畫這一列所有六角柱
    畫這一列上「站著」的東西：樹、丘陵、山、村落、城市、單位
最後畫：城市名牌、移動路線、UI（永遠在最上層）
```

因為後面的東西先畫、前面的後畫，前面的山會自然擋住後面的單位，不需要做任何深度排序。

## Step 5：滑鼠點選要考慮高度

高的格子畫得比平面位置高，只把滑鼠座標反算回平面會點錯格。`screen_to_hex()` 的做法是：先算出一個大概的位置，再檢查附近幾格的**實際頂面多邊形**（`Geometry2D.is_point_in_polygon`），同時命中好幾格時取最前面那一格。

---

## Step 6：預先渲染的 3D 圖（`tools/render_sprites.gd`）

每張圖都是用 Godot 的基本 3D 形狀（球、膠囊、圓柱、圓錐、方塊、稜柱、圓環）拼出來的小模型，再用**跟地圖一樣的正交相機（37°）**拍照：

1. 先用 4 倍解析度渲染，再縮小，邊緣會比較平滑。
2. 在外圍加 1px 深色描邊，放在任何地形上都清楚。
3. 國家色的部件第二次渲染時換成純白、其他部件換成純黑，就得到遮罩 `*_team.png`。遮罩會正確處理前後遮擋。
4. 遊戲裡用第 10b 課的方法上色（灰 × 國家色，保留陰影）。村落的帶子也用同樣方式，套的是部落顏色。

重新產生圖片（需要有視窗和顯示卡的電腦）：

```
cd godot_starter
godot --path . -s res://tools/render_sprites.gd              # 全部
godot --path . -s res://tools/render_sprites.gd -- guard hill  # 只重做指定的圖
```

### 修改或新增一張圖

- **改造型**：修改 `_guard()`、`_mountain()` 這些函式裡的形狀、位置和顏色。要上國家色的部件，最後一個參數傳 `true`（`team`）。
- **新增一張圖**：
  1. 寫一個新函式，加進 `JOBS`（格式：函式名稱、取景高度、輸出像素、旋轉角度）。
  2. 在 `main.gd` 的 `SPRITES` 加上同名的一行，`view` 必須和 `JOBS` 的取景高度一樣。
  3. 用 `_draw_sprite("名稱", 地面點, r, 縮放, 顏色)` 畫出來。
- **換成自己的模型**：也可以用 Blender 做模型，放進同一個場景，用同一台相機拍。只要角度（`PITCH`）一致，就能跟地圖完美對齊。

---

## Step 7：效能

- 地圖大多時間是靜止的，`_process()` 只在「畫面內容有變化」時（鏡頭、滑鼠停留的格子、選取、訊息、回合、單位移動）才重畫。
- 只畫畫面內的列；縮得很小時，每格森林的樹會減半，也不畫樹影。

## 小練習

1. 把 `PITCH` 改成 45°，並把 `main.gd` 的 `TILT`、`RISE` 改成 sin 45°、cos 45°，重新渲染，看看視角的差別。
2. 在 `_draw_tile()` 讓海水顏色隨時間輕微變化（記得把時間加進 `view_sig`，不然畫面不會重畫）。
3. 用 `render_sprites.gd` 做一個「農場小屋」，畫在有農場的格子上。
