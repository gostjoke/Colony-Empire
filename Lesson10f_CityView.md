# Colony Empire 教學 — 第 10f 課
## 城市畫面：2.5D 城市模型、建築清單、點空地就能蓋

**打開方式**：在地圖上**雙擊**自己的城市，或是選取城市後按 **C**，或點城市面板上的「City View」按鈕。**Esc** 關閉。

| 區塊 | 內容 |
|---|---|
| **中間：城市模型** | 一塊六角形的城市底座，上面是**你擁有的建築**（3D 預渲染），每 1 人口就有 1 棟房子（最多 10 棟） |
| | 正在蓋的建築會顯示成**鷹架＋進度條**；還沒蓋的是**空地（＋）**，點一下就開始建造 |
| | 滑鼠移到建築上會變亮，並顯示名稱和效果 |
| **左邊** | 成長（糧食進度、幾回合後長大）、目前生產（縮圖、進度、購買）、每回合產出明細（地塊／建築／消耗）、市民 |
| **右邊** | **已擁有的建築**（縮圖＋效果）、可以蓋的建築、可以訓練的單位（成本、回合數、說明） |
| **上方** | 城市名、人口、疆界、建築數；Prev / Next 可以切換到其他城市；頂部資源列保持可見 |

---

## Step 1：更新

1. 貼上新的 `main.gd`、`hud.gd`、`tools/render_sprites.gd`。
2. `assets/sprites/` 新增 15 張圖（`b_*.png`：底座、市政廳、糧倉、工坊、市場、學校、鷹架、空地、兩種房子，以及國家色遮罩）。

---

## Step 2：城市模型怎麼做的

跟地圖一樣，**全部是 3D 小模型從同一個 37° 相機拍下來的 2.5D 圖**，只是比例不同：

- 地圖上的圖：1 單位 = 一個六角格的半徑。
- 城市畫面的圖（`b_*`）：1 單位 = 城市底座的半徑。

| 圖 | 內容 | 國家色 |
|---|---|---|
| `b_plate` | 六角形草地底座：石板廣場、水井、道路、石牆、城門、花圃 | — |
| `b_townhall` | 市政廳：柱廊、鐘樓、旗子 | 屋頂、塔頂、旗子 |
| `b_granary` | 紅色穀倉＋石造筒倉＋乾草捆 | 筒倉頂 |
| `b_workshop` | 石造工坊、冒煙的煙囪、發光的鍛爐門、鐵砧、木材堆 | 屋頂 |
| `b_market` | 三個攤位，擺著水果和木桶 | 條紋遮棚 |
| `b_school` | 白色校舍、鐘樓、白色木柵欄 | 屋頂、塔頂 |
| `b_scaffold` / `b_plot` | 施工鷹架／圍繩的空地 | — |
| `b_house_a/b` | 兩種民房（紅瓦白牆、石板瓦木牆） | — |

`render_sprites.gd` 的 `JOBS` 多了第 5 個欄位「錨點」：底座的原點放在圖片正中間（0.5），建築放在 84%。

要把城市底座上的位置 `(x, z)` 換算成畫面座標，用的是跟地圖一樣的公式：

```gdscript
var g := p + Vector2(x * r, z * TILT * r) - Vector2(0, PLATE_TOP * RISE * r)
```

建築依 `z` 由後往前畫，前面的會擋住後面的。

---

## Step 3：滑鼠互動

- 每一幀畫圖時，順便把每棟建築的「可點範圍」（地面上方的一塊矩形）記進 `cv_objs`。
- 滑鼠移動時**從最前面往後找**，第一個命中的就是被指到的建築。
- 點到空地時送出 `KEY_1 + 建造序號`，跟按鍵盤一樣，所以規則只寫一次。

## Step 4：擺放位置是資料

```gdscript
const CV_BUILD_SLOTS := { "granary": Vector2(-0.5, -0.08), "workshop": Vector2(0.52, -0.08), ... }
const CV_HOUSES := [Vector2(-0.34, -0.68), ...]   # 依人口一棟一棟填上去
```

之後要加新建築（經濟設計裡的紡織坊、蒸餾坊、碼頭…），步驟是：

1. 在 `render_sprites.gd` 寫一個 `_b_xxx()` 模型。
2. 加進 `JOBS`，重新渲染。
3. 在 `CV_BUILD_SLOTS` 給它一個位置。

---

## 小練習

1. 沿海城市在底座的一側加一座碼頭和一艘小船（新增 `b_dock`，城市 `_coastal()` 時才畫）。
2. 人口超過 10 時，把最舊的兩棟房子換成一棟兩層樓的大房子（新增 `b_house_big`）。
3. 在底座上加幾個會走動的小市民（每幀沿著道路移動的小圖）。
